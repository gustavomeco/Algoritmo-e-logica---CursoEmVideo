algoritmo "creusa conversor f to c "
                         //dar tab
var
   c,f : real
inicio
      escreva ("quantos de temperatura (F) esta no local? ")
      leia(f)
      c <- (f - 32)/1.8
      escreva ("creusa: esta ",c," graus celsius")

fimalgoritmo
